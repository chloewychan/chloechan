{
  "name": "chloechan.ca",
  "version": "1.0.0",
  "description": "Personal portfolio website",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && http-server -p 8000 -o",
    "dev": "npm run build && http-server -p 8000 -c-1",
    "optimize-images": "node scripts/optimize-images.mjs"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "sharp": "^0.35.3",
    "typescript": "^5.3.3"
  }
}
